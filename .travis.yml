sudo: required

services:
  - docker

install:
  - docker build --tag bodsch/docker-dashing .
  - docker run --detach --name dashing bodsch/docker-dashing

script:
  - docker ps | grep -q dashing

after_success:
  - |
    [ "${TRAVIS_BRANCH}" == "master" ] && curl \
      --request POST \
      --header "Content-Type: application/json"  \
      --data '{"source_type":"Branch","source_name":"master"}' \
      https://registry.hub.docker.com/u/bodsch/docker-dashing/trigger/${DOCKER_HUB_TOKEN}/
